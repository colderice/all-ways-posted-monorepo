{
  "name": "all-ways-posted-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "cd audit-app && node server.js",
    "build": "cd audit-app && npm install"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
